Program Fibonacci {
    Function Fib(n: Int) -> Int {
        If (n <= 1) {
            Return n
        }
        Return Fib(n - 1) + Fib(n - 2)
    }
    
    Loop (i from 0 to 10) {
        Print("Fib(")
        Print(i)
        Print(") = ")
        PrintLine(Fib(i))
    }
}
